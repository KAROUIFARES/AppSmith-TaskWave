export default {
// Assuming you have a pagination component or logic in your application

// Function to handle page change event
onPageChange(newPage) {
  // Perform actions based on the new page
  console.log('Page changed:', newPage);
  // Additional logic...
}

// Simulating a page change
const newPage = 2; // The new page number
onPageChange(newPage); // Trigger the onPageChange event with the new page number

}